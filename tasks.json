{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "/usr/bin/gfortran",
            "args": [
                "-g",
                "-Wall",
                "${fileDirname}/${fileBasename}"
            ]
        },
        {
            "type": "shell",
            "label": "testecho",
            "command": "echo ${fileDirname}/${fileBasenameNoExtension}",
            "problemMatcher": []
        }
    ],
    "version": "2.0.0"
}